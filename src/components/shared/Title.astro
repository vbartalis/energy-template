---
export interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<h1 class=`text-4xl font-bold leading-tight mb-4 ${className}`>
  <slot />
</h1>
<!-- <h1
  class=`text-heading-1 font-semibold text-2xl sm:text-3xl md:text-4xl ${className}`
>
  <slot />
</h1> -->
