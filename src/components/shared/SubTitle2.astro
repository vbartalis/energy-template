---
export interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<p
  class={` leading-tight mb-2 text-lg md:text-xl font-semibold text-accent-dark  ${className}`}
>
  <slot />
</p>
